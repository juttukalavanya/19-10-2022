//program on swapping two numbers using pointers
#include<stdio.h>
int main()
{
    int a=20, b=60,temp;
    int *p,*q;
    p=&a;
    q=&b;
    printf("before swapping :%d %d \n ",*p,*q);
    temp=*p;
    *p=*q;
    *q=temp;
    printf("after swapping :%d %d ",*p,*q);
}
output:
before swapping :20 60 
 after swapping :60 20 

